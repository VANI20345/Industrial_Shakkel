# تقرير جاهزية النشر — Shakkel / شَكَّل
تاريخ المراجعة: 2026-05-28

> هذا التقرير يحدّد ما تم إصلاحه الآن، ما يحتاج تشغيل/إعداد يدوي قبل النشر، وما يتطلب اشتراكًا في خدمات خارجية.

---

## ✅ ما تم إصلاحه في هذه الجولة

| # | المشكلة | الإصلاح |
|---|---------|---------|
| 1 | نص زر "قائمة التسعير" يختفي عند الـ hover | `outline` كان يكتسب `hover:text-accent-foreground` افتراضي (أبيض على أبيض) — تمت إضافة `hover:!text-primary` صريحًا في `Header.tsx`. |
| 2 | صفحة "من نحن" تحتوي أرقامًا غير معتمدة (8 دول، +1200 عميل…) | إعادة بناء كاملة لـ `src/pages/About.tsx`: قسم Hero + مهمة/رؤية + قيم + خطوات العمل + وعود — كلها مناسبة لمشروع جديد، بدون أرقام مزعومة. |
| 3 | الإشعارات لا تعمل لحظيًا | إضافة Realtime عبر Supabase channels: `useUnreadReplies` يتفاعل فورًا مع أي رد جديد، `AdminLayout` و`AdminQuotes` و`AdminMessages` يتحدثون لحظيًا عند وصول طلب/رسالة جديدة. |
| 4 | حالة طلب التسعير تتعطل بعد تحديثها | `load()` لم يعد يعيد المؤشر إلى loading=true عند التحديث الصامت بعد العملية، مع اشتراك Realtime. كان السلوك يبدو كقفل لأن الجدول كله كان يومض إلى Loader. |
| 5 | تصميم رسومات Analytics غير أنيق | إعادة تصميم كاملة: 4 KPIs أساسية + بطاقة معدّل تحويل + KPIs ثانوية + AreaChart للاتجاه + Pie أنحف + Bar أفقية بشرائط دائرية. ألوان موحّدة من design tokens. |

---

## ⚠️ يجب على المالك تنفيذها يدويًا قبل النشر

1. **تشغيل ملفات SQL الموجودة بالمشروع:**
   - `SETUP.sql` — يُنشئ جداول `cities` و `site_settings` المطلوبة لصفحات الإدارة.
   - `MIGRATION_2026-05-17.sql` — يضيف `user_id`/`status`/`is_read_by_user` إلى `contact_messages`، وينشئ جدول `contact_message_replies` + RPC `create_quote_with_items`.
   - **خطوة:** افتح Lovable Cloud → SQL Editor → الصق كل ملف وشغّله.

2. **تفعيل Realtime على الجداول التالية** (Cloud → Database → Replication):
   - `quote_requests`
   - `contact_messages`
   - `contact_message_replies`
   بدون ذلك، شارة الإشعارات تعمل على polling فقط (تأخير ~60 ثانية).

3. **`.gitignore`** غير قابل للتعديل من الكود. أضف يدويًا:
   ```
   .env
   .env.*
   !.env.example
   ```

4. **تعيين أول مستخدم Admin** عن طريق `user_roles` (موجود في `SETUP.sql` أو يدويًا):
   ```sql
   INSERT INTO public.user_roles(user_id, role) VALUES ('<UID>', 'admin');
   ```

5. **إعدادات SEO** في `index.html` — تأكد من `meta description`، Open Graph، favicon قبل النشر.

---

## 💳 ما يتطلب اشتراكًا خارجيًا (Optional حسب الاحتياج)

| الميزة | المزود | الحالة الحالية | متى تحتاجها |
|--------|--------|----------------|-------------|
| **إيميل المعاملات** (تأكيد تسعير، رد على رسالة، إعادة كلمة المرور) | Resend / Lovable Emails | Edge Function `send-quote-email` جاهزة لكن تحتاج `RESEND_API_KEY` ودومين موثّق | فور إطلاق التواصل التلقائي مع العميل |
| **نطاق مخصّص** (shakkel.com بدل lovable.app) | Lovable Pro + DNS | غير مفعّل | قبل التسويق الرسمي |
| **WhatsApp Business API** (إرسال تلقائي بدل فتح wa.me) | Meta Cloud API / Twilio | غير مفعّل (نستخدم `wa.me` فقط) | عند الحاجة لأتمتة إشعارات WhatsApp |
| **مراقبة الأخطاء** | Sentry / LogRocket | غير مفعّل | بعد أول 100 مستخدم |
| **تحليلات الزوار** | Plausible / GA4 | غير مفعّل | للقياس التسويقي |
| **CDN/تخزين سعة كبيرة للصور** | Lovable Cloud Storage (مفعّل ضمن الخطة) أو Cloudflare R2 | الحالي كافٍ للبداية | عند تجاوز كتالوج 5000 منتج |
| **النسخ الاحتياطي اليومي** | Supabase Pro | غير مفعّل | فور أول صفقة حقيقية |

---

## 🛡️ تدقيق أمني سريع

| البند | الحالة |
|-------|--------|
| RLS مفعّل على كل الجداول الحساسة | ✅ |
| `user_roles` منفصل عن `profiles` (بدون privilege escalation) | ✅ |
| `has_role` SECURITY DEFINER سليم | ✅ |
| `RequireAdmin` على كل صفحات `/admin/*` | ✅ مفحوص |
| سياسات INSERT لها `WITH CHECK` صحيح | ✅ |
| كلمات السر: حد أدنى 8 + حرف + رقم | ✅ |
| Anti-spam على نموذج التواصل (throttle محلي + تحقق روابط) | ✅ |
| إخفاء `stock_qty` الدقيق عن الزوار (بدل ذلك badges) | ✅ |
| Service Role Key محصور في Edge Functions | ✅ |
| Public anon key مُستخدم فقط في العميل | ✅ |
| `.env` خارج Git (يحتاج تعديل يدوي لـ`.gitignore` — انظر أعلاه) | ⚠️ |

---

## 🧪 تجربة المستخدم — اختبارات يدوية موصى بها قبل الإطلاق

1. تسجيل حساب جديد → استلام إيميل التحقق (يتطلب إعداد البريد).
2. تقديم طلب تسعير كزائر → ظهوره في `/admin/quotes` خلال ثانيتين عبر Realtime.
3. تقديم رسالة تواصل كعميل مسجّل → ظهور الرد من الأدمن في `/my-messages` مع شارة الجرس فورًا.
4. تغيير حالة طلب من `new` → `under_review` → `deal_completed` → التأكد من خصم المخزون مرة واحدة فقط.
5. رفع شعار من `/admin/settings` (بعد تشغيل `SETUP.sql`).
6. تصدير CSV / Excel / PDF من صفحة الطلبات.
7. تبديل اللغة AR ⇄ EN على كل الصفحات الرئيسية.
8. الاختبار على هاتف 375px وعلى شاشة 1920px.

---

## 📊 تقييم الجاهزية الإجمالي

| المحور | النسبة |
|--------|--------|
| الواجهة وتجربة المستخدم | **92%** |
| الأمان وRLS | **95%** |
| البنية الخلفية والمنطق | **90%** |
| إعداد البريد والإشعارات الخارجية | **40%** — يحتاج إعداد |
| الاختبارات الآلية | **15%** — موجود اختباران فقط |
| الجاهزية للنشر التجاري | **80%** بشرط تنفيذ الخطوات اليدوية أعلاه |

---

## 🚀 توصية

النظام **جاهز للنشر التجريبي (Soft Launch)** بعد تنفيذ النقاط 1، 2، 3، 4 من القسم "يجب على المالك تنفيذها يدويًا".

النشر الإنتاجي الكامل يستحسن بعد:
- ربط دومين مخصص.
- تفعيل إيميل المعاملات (Lovable Emails أو Resend مع دومين موثّق).
- تفعيل Sentry لرصد الأخطاء.
- نسخ احتياطي يومي مفعّل.
